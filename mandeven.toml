[llm]
default = "mistral/mistral-small"

[llm.mistral.mistral-small]
model_name         = "mistral-small-latest"
max_context_window = 256000
max_tokens         = 4096
temperature        = 0.7
timeout_secs       = 30

[agent]
# max_iterations = 30   # None ⇒ inner loop has no upper bound
