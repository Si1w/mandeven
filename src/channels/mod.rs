//! Channels — placeholder for the channel registry that fans inbound
//! and outbound [`crate::bus`] traffic across per-source adapters
//! (CLI, TUI, cron, heartbeat, …). Not yet wired into `lib.rs`.
