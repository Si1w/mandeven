//! CLI channel — placeholder. Will host the interactive stdin-based
//! adapter that feeds [`crate::bus::InboundPayload::UserInput`] and
//! renders [`crate::bus::OutboundPayload`] back to the terminal. Not
//! yet wired into `lib.rs`; the current `main.rs` contains an inline
//! bootstrap demo instead.
